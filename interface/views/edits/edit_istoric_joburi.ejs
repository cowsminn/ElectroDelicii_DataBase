<!-- <form action="/istoric_joburi/edit/<%= istoric.data_angajare %>" method="post">

    <label for="angajat">Angajat:</label>
    <input type="number" id="angajat" name="angajat" value="<%= istoric.angajat %>" required>

    <label for="nume_angajat">Nume_angajat:</label>
    <input type="text" id="nume_angajat" name="nume_angajat" value="<%= istoric.nume_angajat %>" required>

    <label for="job">Job:</label>
    <input type="number" id="job" name="job" value="<%= istoric.job %>" required>

    <label for="data_demisie">Data_Demisie:</label>
    <input type="date" id="data_demisie" name="data_demisie" value="<%= istoric.data_demisie?.toISOString()?.split('T')[0] || '' %>" required>
    
    <button type="submit">Save Changes</button>
</form> -->
